"use strict";

var glutApp = angular.module("glutApp", [ "ngResource", "ngRoute", "glutControllers", "glutServices", "ui.bootstrap" ]);

glutApp.config([ "$routeProvider", function(a) {
    a.when("/", {
        templateUrl: "partials/main",
        controller: "MainCtrl"
    }).otherwise({
        redirectTo: "/"
    });
} ]), $(document).ready(function() {
    $(document).on("click", "[data-toggle=offcanvas]", function() {
        $(".row-offcanvas").toggleClass("active");
    });
});

var glutControllers = angular.module("glutControllers", []);

glutControllers.controller("MainCtrl", [ "$scope", "factoryLatLng", function(a, b) {
    console.log("glutControllers and MainCtrl loaded"), a.search = {}, a.search.radiusUnit = "Km", 
    a.changeRadiusUnit = function(b) {
        a.search.radiusUnit = b;
    }, a.getGeo = function(c) {
        console.log("getGeo-", c), b.getLatLng(c).then(function(b) {
            a.search.latitude = b.latitude, a.search.longitude = b.longitude;
        });
    }, a.queryAddresses = function(a) {
        return b.queryAddresses(a);
    };
} ]);

var glutServices = angular.module("glutServices", []);

glutServices.factory("factoryLatLng", [ "$http", function(a) {
    return {
        getLatLng: function(b) {
            return console.log("factory getLatLng-", b), a.get("/getLatLng", {
                params: {
                    address: b
                }
            }).then(function(a) {
                return console.log(a), a.data;
            });
        },
        queryAddresses: function(b) {
            return a.get("http://maps.googleapis.com/maps/api/geocode/json", {
                params: {
                    address: b,
                    sensor: !1
                }
            }).then(function(a) {
                var b = [];
                return angular.forEach(a.data.results, function(a) {
                    b.push(a.formatted_address);
                }), b;
            });
        }
    };
} ]);
//# sourceMappingURL=app.min.map