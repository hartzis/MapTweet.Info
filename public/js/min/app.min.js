"use strict";

var glutApp = angular.module("glutApp", [ "ngResource", "ngRoute", "glutSearchControllers", "glutResultsControllers", "glutLatLngServices", "glutTwitterSearchServices", "ui.bootstrap" ]);

glutApp.config([ "$routeProvider", function(a) {
    a.when("/", {
        templateUrl: "partials/main",
        controller: "searchCtrl"
    }).when("/search/:searchId", {
        templateUrl: "partials/searchResults",
        controller: "resultsCtrl"
    }).otherwise({
        redirectTo: "/"
    });
} ]), $(document).ready(function() {
    $(document).on("click", "[data-toggle=offcanvas]", function() {
        $(".row-offcanvas").toggleClass("active");
    });
});

var glutResultsControllers = angular.module("glutResultsControllers", []);

glutResultsControllers.controller("resultsCtrl", [ "$scope", "$routeParams", function(a, b) {
    console.log("loaded resultsCtrl"), a.searchId = b.searchId, a.results = {}, a.results.tweets = [ {
        id: 0x665360b78024000,
        id_str: "460833959761297408",
        text: "@luna_nando lmfao just a lil bit"
    }, {
        id: 0x66535c387c24000,
        id_str: "460833650787893248",
        text: "Telecomm Reform Package passes Senate. Lots of bipartisan support & leadership from @hickforco!"
    }, {
        id: 0x66535b09e421000,
        id_str: "460833569560989697",
        text: "@MeganZamani Hahahahahaha ❤️❤️"
    } ];
} ]);

var glutSearchControllers = angular.module("glutSearchControllers", []);

glutSearchControllers.controller("searchCtrl", [ "$scope", "factoryLatLng", "factoryTwitterSearch", function(a, b) {
    console.log("glutControllers and MainCtrl loaded"), a.search = {}, a.search.radiusUnit = "Km", 
    a.search.useCurrentLocation = "Use My Location", a.changeRadiusUnit = function(b) {
        a.search.radiusUnit = b;
    }, a.getGeo = function(c) {
        b.getLatLng(c).then(function(b) {
            a.search.latitude = b.latitude, a.search.longitude = b.longitude;
        });
    }, a.queryAddresses = function(a) {
        return b.queryAddresses(a);
    }, a.getCurrentLocation = function() {
        b.getCurrentLocation(function(b) {
            b.notSupported ? a.search.useCurrentLocation = "Unable to use My Location" : (a.search.latitude = b.latitude, 
            a.search.longitude = b.longitude, a.$apply());
        });
    };
} ]);

var glutLatLngServices = angular.module("glutLatLngServices", []);

glutLatLngServices.factory("factoryLatLng", [ "$http", function(a) {
    return {
        getLatLng: function(b) {
            return a.get("/getLatLng", {
                params: {
                    address: b
                }
            }).then(function(a) {
                return a.data;
            });
        },
        queryAddresses: function(b) {
            return a.get("http://maps.googleapis.com/maps/api/geocode/json", {
                params: {
                    address: b,
                    sensor: !1
                }
            }).then(function(a) {
                var b = [];
                return angular.forEach(a.data.results, function(a) {
                    b.push(a.formatted_address);
                }), b;
            });
        },
        getCurrentLocation: function(a) {
            navigator.geolocation ? navigator.geolocation.getCurrentPosition(function(b) {
                a(b.coords);
            }) : a({
                notSupported: !0
            });
        }
    };
} ]);

var glutTwitterSearchServices = angular.module("glutTwitterSearchServices", []);

glutTwitterSearchServices.factory("factoryTwitterSearch", [ "$http", function() {
    return {
        postTwitterSearch: function(a) {
            console.log(a);
        }
    };
} ]);
//# sourceMappingURL=app.min.map