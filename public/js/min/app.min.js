"use strict";

var glutApp = angular.module("glutApp", [ "ngResource", "ngRoute", "glutControllers" ]);

glutApp.config([ "$routeProvider", function(a) {
    a.when("/", {
        templateUrl: "partials/main",
        controller: "MainCtrl"
    }).otherwise({
        redirectTo: "/"
    });
} ]);

var glutControllers = angular.module("glutControllers", []);

glutControllers.controller("MainCtrl", [ "$scope", "$http", function() {
    console.log("glutControllers and MainCtrl loaded");
} ]);
//# sourceMappingURL=app.min.map